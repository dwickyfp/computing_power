# Application Configuration
APP_NAME=Rosetta ETL Platform
APP_VERSION=1.0.0
APP_ENV=development
DEBUG=True
LOG_LEVEL=INFO

# API Configuration
API_V1_PREFIX=/api/v1
HOST=0.0.0.0
PORT=8000

# Database Configuration (Application Metadata DB)
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/rosetta_metadata
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=5
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO=False

# Connection Pool Settings
DB_POOL_PRE_PING=True
DB_POOL_USE_LIFO=True

# Security
SECRET_KEY=your-secret-key-change-in-production
API_KEY=your-api-key-change-in-production
CREDENTIAL_ENCRYPTION_KEY=your-32-byte-base64-key-change-in-production
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# WAL Monitoring Configuration
WAL_MONITOR_ENABLED=True
WAL_MONITOR_INTERVAL_SECONDS=300
WAL_MONITOR_TIMEOUT_SECONDS=30
WAL_MONITOR_MAX_RETRIES=3

# Background Tasks
BACKGROUND_TASK_ENABLED=True
SCHEDULER_TIMEZONE=Asia/Jakarta

# Redis Configuration
REDIS_URL=redis://rosetta-redis:6379/0

# Celery Configuration (for worker integration)
CELERY_BROKER_URL=redis://rosetta-redis:6379/1
CELERY_RESULT_BACKEND=redis://rosetta-redis:6379/2

# Logging
LOG_FORMAT=json
LOG_FILE_PATH=logs/app.log
LOG_FILE_MAX_BYTES=10485760
LOG_FILE_BACKUP_COUNT=5

# Metrics & Monitoring
ENABLE_METRICS=True
METRICS_PORT=9090

# Rate Limiting
RATE_LIMIT_ENABLED=True
RATE_LIMIT_PER_MINUTE=60

# External Services
COMPUTE_NODE_URL=http://localhost:8001
WORKER_HEALTH_URL=http://localhost:8002

WORKER_ENABLED=true